#include "main.h"

/**
 * mul - Function to multiply two integers together
 * @a: The first integer to be multiplied
 * @b: The second integer to be multiplied
 * Descirption: Two inputs, one multiplication, one result, simple
 * Return: It returns the result of multiplication
*/

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
